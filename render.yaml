services:
  - type: web
    name: mvp-numble-backend
    env: python
    buildCommand: ./render-build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
    buildFilter:
      paths:
        - backend/**/*
